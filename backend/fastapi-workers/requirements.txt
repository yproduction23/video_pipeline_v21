fastapi==0.115.0
uvicorn[standard]==0.32.0
redis==5.2.0
pydantic==2.9.2
python-dotenv==1.0.1
faster-whisper==1.1.0
rapidfuzz==3.10.0
ffmpeg-python==0.2.0
boto3==1.35.0
httpx==0.27.2
requests==2.32.3
fal-client>=0.5.0          # Fal.ai CDN upload + Kling inference (upload_file, subscribe)
python-multipart==0.0.12
gTTS==2.5.1
anthropic==0.40.0
opencv-python-headless==4.10.0.84
matplotlib==3.9.2
numpy>=1.24.0,<2.0
Pillow>=10.0.0
# Approved real-person photos are cut out locally and cached; no face is sent
# to an image-generation provider.
# 2.0.60 supports the existing NumPy 1.x scientific stack (newer releases
# require NumPy >=2.3). We use the in-process library API only, never rembg's
# HTTP server / custom model endpoint.
rembg[cpu]==2.0.60
# DOM-native article quote coordinates and reproducible public-page captures.
playwright==1.49.1

# ─── 신규: 한국 주식 시장 데이터 ───────────────────────
pykrx<1.2.7
finance-datareader>=0.9.50
opendartreader<=0.2.1

# ─── 신규: 한국어 NLP 키워드 추출 ────────────────────────
kiwipiepy>=0.17.0
feedparser>=6.0.11
beautifulsoup4>=4.12.3
scikit-learn>=1.5.0

# ─── 신규: 미국 주식 시장 데이터 ───────────────────────
finnhub-python>=2.4.20
yfinance>=0.2.38
fredapi>=0.5.2

# ─── 신규: 데이터 처리 ──────────────────────────────
pandas>=2.2.0

# ─── 신규: 정밀 음성-자막 싱크 (cross-attention 기반) ──────
stable-ts[fw]>=2.17.0

# Keep the documented container regression-test command executable.
pytest>=8.3.0,<9.0
youtube-transcript-api>=1.2,<2
